{% extends "layout.twig" %}

{% block content %}
    <ul>
        {% for post in posts %}
            <li>{{ post.title }} / <a href="/post/{{ post.id }}/pdf">PDF</a></li>
        {% endfor %}
    </ul>
{% endblock %}
